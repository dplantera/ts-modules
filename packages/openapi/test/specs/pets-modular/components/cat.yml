openapi: 3.0.3
paths:
components:
  schemas:
    Cat:
      allOf:
        - $ref: './pet.yml#/components/schemas/PetBase'
        - properties:
            breed:
              type: string
              enum: [ 'CAT_A', 'CAT_B' ]